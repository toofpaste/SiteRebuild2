export class Posts{
  // tslint:disable-next-line:max-line-length
  constructor(public title: string, public time: number, public username: string, public commentNum: number, public comments: string[], public upvotes: number, public link: string, public thumbnail: string) {};
}
